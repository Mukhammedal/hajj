import { LoadingState } from "@/components/shell/fallback-state";

export default function CrmLoading() {
  return <LoadingState copy="Собираем список паломников, групп и операционные метрики сезона." title="Открываем <em>CRM</em>" />;
}
