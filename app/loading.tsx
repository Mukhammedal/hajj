import { LoadingState } from "@/components/shell/fallback-state";

export default function RootLoading() {
  return <LoadingState copy="Собираем маршрут, шрифты и данные для текущего экрана." title="Открываем <em>HajjCRM</em>" />;
}
